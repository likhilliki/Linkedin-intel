packages:
  "framer-motion": "For page transitions, staggered list reveals, and modal animations"
  "recharts": "For dashboard statistics visualization"
  "date-fns": "For formatting timestamps beautifully"
  "clsx": "For conditional class merging"
  "tailwind-merge": "For merging tailwind classes safely"

images:
  - path: "artifacts/linkedin-dashboard/public/images/hero-mesh.png"
    description: "Deep dark navy and electric blue abstract gradient mesh with very subtle data node network points, premium tech aesthetic"
    aspect_ratio: "16:9"

notes:
  - The frontend uses the @workspace/api-client-react hooks generated by Orval.
  - The UI is forced into a dark-mode premium SaaS aesthetic.
